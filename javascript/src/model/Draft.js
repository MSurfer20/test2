/**
 * Zulip REST API
 * Powerful open source group chat 
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The Draft model module.
 * @module model/Draft
 * @version 1.0.0
 */
class Draft {
    /**
     * Constructs a new <code>Draft</code>.
     * A dictionary for representing a message draft. 
     * @alias module:model/Draft
     * @param type {module:model/Draft.TypeEnum} The type of the draft. Either unaddressed (empty string), \"stream\", or \"private\" (for PMs and private group messages). 
     * @param to {Array.<Number>} An array of the tentative target audience IDs. For \"stream\" messages, this should contain exactly 1 ID, the ID of the target stream. For private messages, this should be an array of target user IDs. For unaddressed drafts, this is ignored, and clients should send an empty array. 
     * @param topic {String} For stream message drafts, the tentative topic name. For private or unaddressed messages, this will be ignored and should ideally be the empty string. Should not contain null bytes. 
     * @param content {String} The body of the draft. Should not contain null bytes. 
     */
    constructor(type, to, topic, content) { 
        
        Draft.initialize(this, type, to, topic, content);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, type, to, topic, content) { 
        obj['type'] = type;
        obj['to'] = to;
        obj['topic'] = topic;
        obj['content'] = content;
    }

    /**
     * Constructs a <code>Draft</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Draft} obj Optional instance to populate.
     * @return {module:model/Draft} The populated <code>Draft</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Draft();

            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'Number');
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
            if (data.hasOwnProperty('to')) {
                obj['to'] = ApiClient.convertToType(data['to'], ['Number']);
            }
            if (data.hasOwnProperty('topic')) {
                obj['topic'] = ApiClient.convertToType(data['topic'], 'String');
            }
            if (data.hasOwnProperty('content')) {
                obj['content'] = ApiClient.convertToType(data['content'], 'String');
            }
            if (data.hasOwnProperty('timestamp')) {
                obj['timestamp'] = ApiClient.convertToType(data['timestamp'], 'Number');
            }
        }
        return obj;
    }


}

/**
 * The unique ID of the draft. It will only used whenever the drafts are fetched. This field should not be specified when the draft is being created or edited. 
 * @member {Number} id
 */
Draft.prototype['id'] = undefined;

/**
 * The type of the draft. Either unaddressed (empty string), \"stream\", or \"private\" (for PMs and private group messages). 
 * @member {module:model/Draft.TypeEnum} type
 */
Draft.prototype['type'] = undefined;

/**
 * An array of the tentative target audience IDs. For \"stream\" messages, this should contain exactly 1 ID, the ID of the target stream. For private messages, this should be an array of target user IDs. For unaddressed drafts, this is ignored, and clients should send an empty array. 
 * @member {Array.<Number>} to
 */
Draft.prototype['to'] = undefined;

/**
 * For stream message drafts, the tentative topic name. For private or unaddressed messages, this will be ignored and should ideally be the empty string. Should not contain null bytes. 
 * @member {String} topic
 */
Draft.prototype['topic'] = undefined;

/**
 * The body of the draft. Should not contain null bytes. 
 * @member {String} content
 */
Draft.prototype['content'] = undefined;

/**
 * A Unix timestamp (seconds only) representing when the draft was last edited. When creating a draft, this key need not be present and it will be filled in automatically by the server. 
 * @member {Number} timestamp
 */
Draft.prototype['timestamp'] = undefined;





/**
 * Allowed values for the <code>type</code> property.
 * @enum {String}
 * @readonly
 */
Draft['TypeEnum'] = {

    /**
     * value: ""
     * @const
     */
    "empty": "",

    /**
     * value: "stream"
     * @const
     */
    "stream": "stream",

    /**
     * value: "private"
     * @const
     */
    "private": "private"
};



export default Draft;

