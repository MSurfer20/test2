/**
 * Zulip REST API
 * Powerful open source group chat 
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { autoinject } from 'aurelia-framework';
import { HttpClient } from 'aurelia-http-client';
import { Api } from './Api';
import { AuthStorage } from './AuthStorage';
import {
  JsonSuccessBase &amp; object,
  JsonSuccess,
} from './models';

/**
 * createUser - parameters interface
 */
export interface ICreateUserParams {
  email: string;
  password: string;
  fullName: string;
}

/**
 * createUserGroup - parameters interface
 */
export interface ICreateUserGroupParams {
  name: string;
  description: string;
  members: Array<number>;
}

/**
 * deactivateOwnUser - parameters interface
 */
export interface IDeactivateOwnUserParams {
}

/**
 * deactivateUser - parameters interface
 */
export interface IDeactivateUserParams {
  userId: number;
}

/**
 * getAttachments - parameters interface
 */
export interface IGetAttachmentsParams {
}

/**
 * getOwnUser - parameters interface
 */
export interface IGetOwnUserParams {
}

/**
 * getUser - parameters interface
 */
export interface IGetUserParams {
  userId: number;
  clientGravatar?: boolean;
  includeCustomProfileFields?: boolean;
}

/**
 * getUserByEmail - parameters interface
 */
export interface IGetUserByEmailParams {
  email: string;
  clientGravatar?: boolean;
  includeCustomProfileFields?: boolean;
}

/**
 * getUserGroups - parameters interface
 */
export interface IGetUserGroupsParams {
}

/**
 * getUserPresence - parameters interface
 */
export interface IGetUserPresenceParams {
  userIdOrEmail: string;
}

/**
 * getUsers - parameters interface
 */
export interface IGetUsersParams {
  clientGravatar?: boolean;
  includeCustomProfileFields?: boolean;
}

/**
 * muteUser - parameters interface
 */
export interface IMuteUserParams {
  mutedUserId: number;
}

/**
 * reactivateUser - parameters interface
 */
export interface IReactivateUserParams {
  userId: number;
}

/**
 * removeUserGroup - parameters interface
 */
export interface IRemoveUserGroupParams {
  userGroupId: number;
}

/**
 * setTypingStatus - parameters interface
 */
export interface ISetTypingStatusParams {
  op: 'start' | 'stop';
  to: Array<number>;
  type?: 'private' | 'stream';
  topic?: string;
}

/**
 * unmuteUser - parameters interface
 */
export interface IUnmuteUserParams {
  mutedUserId: number;
}

/**
 * updateDisplaySettings - parameters interface
 */
export interface IUpdateDisplaySettingsParams {
  twentyFourHourTime?: boolean;
  denseMode?: boolean;
  starredMessageCounts?: boolean;
  fluidLayoutWidth?: boolean;
  highContrastMode?: boolean;
  colorScheme?: 1 | 2 | 3;
  translateEmoticons?: boolean;
  defaultLanguage?: string;
  defaultView?: string;
  leftSideUserlist?: boolean;
  emojiset?: string;
  demoteInactiveStreams?: 1 | 2 | 3;
  timezone?: string;
}

/**
 * updateNotificationSettings - parameters interface
 */
export interface IUpdateNotificationSettingsParams {
  enableStreamDesktopNotifications?: boolean;
  enableStreamEmailNotifications?: boolean;
  enableStreamPushNotifications?: boolean;
  enableStreamAudibleNotifications?: boolean;
  notificationSound?: string;
  enableDesktopNotifications?: boolean;
  enableSounds?: boolean;
  enableOfflineEmailNotifications?: boolean;
  enableOfflinePushNotifications?: boolean;
  enableOnlinePushNotifications?: boolean;
  enableDigestEmails?: boolean;
  enableMarketingEmails?: boolean;
  enableLoginEmails?: boolean;
  messageContentInEmailNotifications?: boolean;
  pmContentInDesktopNotifications?: boolean;
  wildcardMentionsNotify?: boolean;
  desktopIconCountDisplay?: 1 | 2 | 3;
  realmNameInNotifications?: boolean;
  presenceEnabled?: boolean;
}

/**
 * updateUser - parameters interface
 */
export interface IUpdateUserParams {
  userId: number;
  fullName?: string;
  role?: number;
  profileData?: Array<object>;
}

/**
 * updateUserGroup - parameters interface
 */
export interface IUpdateUserGroupParams {
  userGroupId: number;
  name: string;
  description: string;
}

/**
 * updateUserGroupMembers - parameters interface
 */
export interface IUpdateUserGroupMembersParams {
  userGroupId: number;
  _delete?: Array<number>;
  add?: Array<number>;
}

/**
 * UsersApi - API class
 */
@autoinject()
export class UsersApi extends Api {

  /**
   * Creates a new UsersApi class.
   *
   * @param httpClient The Aurelia HTTP client to be injected.
   * @param authStorage A storage for authentication data.
   */
  constructor(httpClient: HttpClient, authStorage: AuthStorage) {
    super(httpClient, authStorage);
  }

  /**
   * Create a user
   * {!can-create-users-only.md!}  Create a new user account via the API.  &#x60;POST {{ api_url }}/v1/users&#x60; 
   * @param params.email The email address of the new user. 
   * @param params.password The password of the new user. 
   * @param params.fullName The full name of the new user. 
   */
  async createUser(params: ICreateUserParams): Promise<JsonSuccessBase & object> {
    // Verify required parameters are set
    this.ensureParamIsSet('createUser', params, 'email');
    this.ensureParamIsSet('createUser', params, 'password');
    this.ensureParamIsSet('createUser', params, 'fullName');

    // Create URL to call
    const url = `${this.basePath}/users`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Set query parameters
      .withParams({ 
        'email': params['email'],
        'password': params['password'],
        'full_name': params['fullName'],
      })

      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Create a user group
   * Create a new [user group](/help/user-groups).  &#x60;POST {{ api_url }}/v1/user_groups/create&#x60; 
   * @param params.name The name of the user group. 
   * @param params.description The description of the user group. 
   * @param params.members An array containing the user IDs of the initial members for the new user group. 
   */
  async createUserGroup(params: ICreateUserGroupParams): Promise<JsonSuccess> {
    // Verify required parameters are set
    this.ensureParamIsSet('createUserGroup', params, 'name');
    this.ensureParamIsSet('createUserGroup', params, 'description');
    this.ensureParamIsSet('createUserGroup', params, 'members');

    // Create URL to call
    const url = `${this.basePath}/user_groups/create`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Set query parameters
      .withParams({ 
        'name': params['name'],
        'description': params['description'],
        'members': params['members'],
      })

      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Deactivate own user
   * Deactivates the user\&#39;s account.  See also the administrative endpoint for [deactivating another user](/api/deactivate-user).  &#x60;DELETE {{ api_url }}/v1/users/me&#x60;  This endpoint is primarily useful to Zulip clients providing a user settings UI. 
   */
  async deactivateOwnUser(): Promise<JsonSuccess> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/users/me`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asDelete()

      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Deactivate a user
   * [Deactivates a user](https://zulip.com/help/deactivate-or-reactivate-a-user) given their user ID.  &#x60;DELETE {{ api_url }}/v1/users/{user_id}&#x60; 
   * @param params.userId The target user\&#39;s ID. 
   */
  async deactivateUser(params: IDeactivateUserParams): Promise<JsonSuccess> {
    // Verify required parameters are set
    this.ensureParamIsSet('deactivateUser', params, 'userId');

    // Create URL to call
    const url = `${this.basePath}/users/{user_id}`
      .replace(`{${'user_id'}}`, encodeURIComponent(`${params['userId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asDelete()

      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get attachments
   * Fetch metadata on files uploaded by the requesting user.  &#x60;GET {{ api_url }}/v1/attachments&#x60; 
   */
  async getAttachments(): Promise<JsonSuccessBase & object> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/attachments`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get own user
   * Get basic data about the user/bot that requests this endpoint.  &#x60;GET {{ api_url }}/v1/users/me&#x60; 
   */
  async getOwnUser(): Promise<JsonSuccessBase & object> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/users/me`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get a user
   * Fetch details for a single user in the organization.  &#x60;GET {{ api_url }}/v1/users/{user_id}&#x60;  You can also fetch details on [all users in the organization](/api/get-users) or [by email](/api/get-user-by-email).  *This endpoint is new in Zulip Server 3.0 (feature level 1).* 
   * @param params.userId The target user\&#39;s ID. 
   * @param params.clientGravatar Whether the client supports computing gravatars URLs.  If enabled, &#x60;avatar_url&#x60; will be included in the response only if there is a Zulip avatar, and will be &#x60;null&#x60; for users who are using gravatar as their avatar.  This option significantly reduces the compressed size of user data, since gravatar URLs are long, random strings and thus do not compress well. The &#x60;client_gravatar&#x60; field is set to &#x60;true&#x60; if clients can compute their own gravatars. 
   * @param params.includeCustomProfileFields Whether the client wants [custom profile field](/help/add-custom-profile-fields) data to be included in the response.  **Changes**: New in Zulip 2.1.0.  Previous versions do no offer these data via the API. 
   */
  async getUser(params: IGetUserParams): Promise<JsonSuccessBase & object> {
    // Verify required parameters are set
    this.ensureParamIsSet('getUser', params, 'userId');

    // Create URL to call
    const url = `${this.basePath}/users/{user_id}`
      .replace(`{${'user_id'}}`, encodeURIComponent(`${params['userId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()
      // Set query parameters
      .withParams({ 
        'client_gravatar': params['clientGravatar'],
        'include_custom_profile_fields': params['includeCustomProfileFields'],
      })

      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get a user by email
   * Fetch details for a single user in the organization given a Zulip display email address.  &#x60;GET {{ api_url }}/v1/users/{email}&#x60;  Note that this endpoint uses Zulip display emails addresses for organizations that have configured limited [email address visibility](/help/restrict-visibility-of-email-addresses).  You can also fetch details on [all users in the organization](/api/get-users) or [by user ID](/api/get-user).  Fetching by user ID is generally recommended when possible, as users can [change their email address](/help/change-your-email-address).  *This endpoint is new in Zulip Server 4.0 (feature level 39).* 
   * @param params.email The email address of the user whose details you want to fetch. 
   * @param params.clientGravatar Whether the client supports computing gravatars URLs.  If enabled, &#x60;avatar_url&#x60; will be included in the response only if there is a Zulip avatar, and will be &#x60;null&#x60; for users who are using gravatar as their avatar.  This option significantly reduces the compressed size of user data, since gravatar URLs are long, random strings and thus do not compress well. The &#x60;client_gravatar&#x60; field is set to &#x60;true&#x60; if clients can compute their own gravatars. 
   * @param params.includeCustomProfileFields Whether the client wants [custom profile field](/help/add-custom-profile-fields) data to be included in the response.  **Changes**: New in Zulip 2.1.0.  Previous versions do no offer these data via the API. 
   */
  async getUserByEmail(params: IGetUserByEmailParams): Promise<JsonSuccessBase & object> {
    // Verify required parameters are set
    this.ensureParamIsSet('getUserByEmail', params, 'email');

    // Create URL to call
    const url = `${this.basePath}/users/{email}`
      .replace(`{${'email'}}`, encodeURIComponent(`${params['email']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()
      // Set query parameters
      .withParams({ 
        'client_gravatar': params['clientGravatar'],
        'include_custom_profile_fields': params['includeCustomProfileFields'],
      })

      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get user groups
   * {!api-members-only.md!}  Fetches all of the user groups in the organization.  &#x60;GET {{ api_url }}/v1/user_groups&#x60; 
   */
  async getUserGroups(): Promise<JsonSuccessBase & object> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/user_groups`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get user presence
   * Get the presence status for a specific user.  This endpoint is most useful for embedding data about a user\&#39;s presence status in other sites (E.g. an employee directory).  Full Zulip clients like mobile/desktop apps will want to use the main presence endpoint, which returns data for all active users in the organization, instead.  &#x60;GET {{ api_url }}/v1/users/{user_id_or_email}/presence&#x60;  See [Zulip\&#39;s developer documentation](https://zulip.readthedocs.io/en/latest/subsystems/presence.html) for details on the data model for presence in Zulip. 
   * @param params.userIdOrEmail The user_id or Zulip display email address of the user whose presence you want to fetch.  **Changes**: New in Zulip 4.0 (feature level 43). Previous versions only supported identifying the user by Zulip display email. 
   */
  async getUserPresence(params: IGetUserPresenceParams): Promise<JsonSuccessBase & object> {
    // Verify required parameters are set
    this.ensureParamIsSet('getUserPresence', params, 'userIdOrEmail');

    // Create URL to call
    const url = `${this.basePath}/users/{user_id_or_email}/presence`
      .replace(`{${'user_id_or_email'}}`, encodeURIComponent(`${params['userIdOrEmail']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get all users
   * Retrieve details on all users in the organization.  Optionally includes values of [custom profile field](/help/add-custom-profile-fields).  &#x60;GET {{ api_url }}/v1/users&#x60;  You can also [fetch details on a single user](/api/get-user). 
   * @param params.clientGravatar Whether the client supports computing gravatars URLs.  If enabled, &#x60;avatar_url&#x60; will be included in the response only if there is a Zulip avatar, and will be &#x60;null&#x60; for users who are using gravatar as their avatar.  This option significantly reduces the compressed size of user data, since gravatar URLs are long, random strings and thus do not compress well. The &#x60;client_gravatar&#x60; field is set to &#x60;true&#x60; if clients can compute their own gravatars. 
   * @param params.includeCustomProfileFields Whether the client wants [custom profile field](/help/add-custom-profile-fields) data to be included in the response.  **Changes**: New in Zulip 2.1.0.  Previous versions do no offer these data via the API. 
   */
  async getUsers(params: IGetUsersParams): Promise<JsonSuccessBase & object> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/users`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()
      // Set query parameters
      .withParams({ 
        'client_gravatar': params['clientGravatar'],
        'include_custom_profile_fields': params['includeCustomProfileFields'],
      })

      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Mute a user
   * This endpoint [mutes a user](/help/mute-a-user).  Messages sent by users you\&#39;ve muted will be automatically marked as read and hidden.  &#x60;POST {{ api_url }}/v1/users/me/muted_users/{muted_user_id}&#x60;  Muted users should be implemented by clients as follows:  * The server will immediately mark all messages sent by the muted   user as read.  This will automatically clear any existing mobile   push notifications related to the muted user. * The server will mark any new messages sent by the muted user as read   for your account, which prevents all email and mobile push notifications. * Clients should exclude muted users from presence lists or other UI   for viewing or composing 1:1 private messages. 1:1 private messages sent by   muted users should be hidden everywhere in the Zulip UI. * Stream messages and group private messages sent by the muted   user should avoid displaying the content and name/avatar,   but should display that N messages by a muted user were   hidden (so that it is possible to interpret the messages by   other users who are talking with the muted user). * Group private message conversations including the muted user   should display muted users as \&quot;Muted user\&quot;, rather than   showing their name, in lists of such conversations, along with using   a blank grey avatar where avatars are displayed. * Administrative/settings UI elements for showing \&quot;All users that exist   on this stream or realm\&quot;, e.g. for organization   administration or showing stream subscribers, should display   the user\&#39;s name as normal.  **Changes**: New in Zulip 4.0 (feature level 48). 
   * @param params.mutedUserId The ID of the user to mute/un-mute. 
   */
  async muteUser(params: IMuteUserParams): Promise<JsonSuccess> {
    // Verify required parameters are set
    this.ensureParamIsSet('muteUser', params, 'mutedUserId');

    // Create URL to call
    const url = `${this.basePath}/users/me/muted_users/{muted_user_id}`
      .replace(`{${'muted_user_id'}}`, encodeURIComponent(`${params['mutedUserId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()

      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Reactivate a user
   * [Reactivates a user](https://zulip.com/help/deactivate-or-reactivate-a-user) given their user ID.  &#x60;POST {{ api_url }}/v1/users/{user_id}/reactivate&#x60; 
   * @param params.userId The target user\&#39;s ID. 
   */
  async reactivateUser(params: IReactivateUserParams): Promise<JsonSuccess> {
    // Verify required parameters are set
    this.ensureParamIsSet('reactivateUser', params, 'userId');

    // Create URL to call
    const url = `${this.basePath}/users/{user_id}/reactivate`
      .replace(`{${'user_id'}}`, encodeURIComponent(`${params['userId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()

      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Delete a user group
   * Delete a [user group](/help/user-groups).  &#x60;DELETE {{ api_url }}/v1/user_groups/{user_group_id}&#x60; 
   * @param params.userGroupId The ID of the target user group. 
   */
  async removeUserGroup(params: IRemoveUserGroupParams): Promise<JsonSuccess> {
    // Verify required parameters are set
    this.ensureParamIsSet('removeUserGroup', params, 'userGroupId');

    // Create URL to call
    const url = `${this.basePath}/user_groups/{user_group_id}`
      .replace(`{${'user_group_id'}}`, encodeURIComponent(`${params['userGroupId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asDelete()

      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Set \&quot;typing\&quot; status
   * Notify other users whether the current user is typing a message.  &#x60;POST {{ api_url }}/v1/typing&#x60;  Clients implementing Zulip\&#39;s typing notifications protocol should work as follows:  * Send a request to this endpoint with &#x60;op&#x3D;\&quot;start\&quot;&#x60; when a user starts typing a message,   and also every &#x60;TYPING_STARTED_WAIT_PERIOD&#x3D;10&#x60; seconds that the user continues to   actively type or otherwise interact with the compose UI (E.g. interacting with the   compose box emoji picker). * Send a request to this endpoint with &#x60;op&#x3D;\&quot;stop\&quot;&#x60; when a user pauses using the   compose UI for at least &#x60;TYPING_STOPPED_WAIT_PERIOD&#x3D;5&#x60; seconds or cancels   the compose action (if it had previously sent a \&quot;start\&quot; operation for that   compose action). * Start displaying \&quot;Sender is typing\&quot; for a given conversation when the client   receives an &#x60;op&#x3D;\&quot;start\&quot;&#x60; event from the [events API](/api/get-events). * Continue displaying \&quot;Sender is typing\&quot; until they receive an &#x60;op&#x3D;\&quot;stop\&quot;&#x60; event   from the [events API](/api/get-events) or &#x60;TYPING_STARTED_EXPIRY_PERIOD&#x3D;15&#x60;   seconds have passed without a new &#x60;op&#x3D;\&quot;start\&quot;&#x60; event for that conversation. * Clients that support displaying stream typing notifications (new in Zulip 4.0)   should indicate they support processing stream typing events via the   &#x60;stream_typing_notifications&#x60; in the &#x60;client_capabilities&#x60; parameter to &#x60;/register&#x60;.  This protocol is designed to allow the server-side typing notifications implementation to be stateless while being resilient; network failures cannot result in a user being incorrectly displayed as perpetually typing.  See [the typing notification docs](https://zulip.readthedocs.io/en/latest/subsystems/typing-indicators.html) for additional design details on Zulip\&#39;s typing notifications protocol. 
   * @param params.op Whether the user has started (&#x60;start&#x60;) or stopped (&#x60;stop&#x60;) to type. 
   * @param params.to For \&#39;private\&#39; type it is the user_ids of the recipients of the message being typed. Send a JSON-encoded list of user_ids. (Use a list even if there is only one recipient.)  For \&#39;stream\&#39; type it is a single element list containing ID of stream in which the message is being typed.  **Changes**: Before Zulip 2.0, this parameter accepted only a JSON-encoded list of email addresses.  Support for the email address-based format was removed in Zulip 3.0 (feature level 11). 
   * @param params.type Type of the message being composed. 
   * @param params.topic Topic to which message is being typed. Required for the \&#39;stream\&#39; type. Ignored in case of \&#39;private\&#39; type. 
   */
  async setTypingStatus(params: ISetTypingStatusParams): Promise<JsonSuccess> {
    // Verify required parameters are set
    this.ensureParamIsSet('setTypingStatus', params, 'op');
    this.ensureParamIsSet('setTypingStatus', params, 'to');

    // Create URL to call
    const url = `${this.basePath}/typing`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Set query parameters
      .withParams({ 
        'type': params['type'],
        'op': params['op'],
        'to': params['to'],
        'topic': params['topic'],
      })

      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Unmute a user
   * This endpoint unmutes a user.  &#x60;DELETE {{ api_url }}/v1/users/me/muted_users/{muted_user_id}&#x60;  **Changes**: New in Zulip 4.0 (feature level 48). 
   * @param params.mutedUserId The ID of the user to mute/un-mute. 
   */
  async unmuteUser(params: IUnmuteUserParams): Promise<JsonSuccess> {
    // Verify required parameters are set
    this.ensureParamIsSet('unmuteUser', params, 'mutedUserId');

    // Create URL to call
    const url = `${this.basePath}/users/me/muted_users/{muted_user_id}`
      .replace(`{${'muted_user_id'}}`, encodeURIComponent(`${params['mutedUserId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asDelete()

      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Update display settings
   * This endpoint is used to edit the current user\&#39;s user interface settings.  &#x60;PATCH {{ api_url }}/v1/settings/display&#x60; 
   * @param params.twentyFourHourTime Whether time should be [displayed in 24-hour notation](/help/change-the-time-format). 
   * @param params.denseMode This setting has no effect at present.  It is reserved for use in controlling the default font size in Zulip. 
   * @param params.starredMessageCounts Whether clients should display the [number of starred messages](/help/star-a-message#display-the-number-of-starred-messages). 
   * @param params.fluidLayoutWidth Whether to use the [maximum available screen width](/help/enable-full-width-display) for the web app\&#39;s center panel (message feed, recent topics) on wide screens. 
   * @param params.highContrastMode This setting is reserved for use to control variations in Zulip\&#39;s design to help visually impaired users. 
   * @param params.colorScheme Controls which [color theme](/help/night-mode) to use.  * 1 - Automatic * 2 - Night mode * 3 - Day mode  Automatic detection is implementing using the standard &#x60;prefers-color-scheme&#x60; media query. 
   * @param params.translateEmoticons Whether to [translate emoticons to emoji](/help/enable-emoticon-translations) in messages the user sends. 
   * @param params.defaultLanguage What [default language](/help/change-your-language) to use for the account.  This controls both the Zulip UI as well as email notifications sent to the user.  The value needs to be a standard language code that the Zulip server has translation data for; for example, &#x60;\&quot;en\&quot;&#x60; for English or &#x60;\&quot;de\&quot;&#x60; for German.  **Changes**: Removed unnecessary JSON-encoding of parameter in Zulip 4.0 (feature level 63). 
   * @param params.defaultView The [default view](/help/change-default-view) used when opening a new Zulip web app window or hitting the &#x60;Esc&#x60; keyboard shortcut repeatedly.  * \&quot;recent_topics\&quot; - Recent topics view * \&quot;all_messages\&quot; - All messages view  **Changes**: Removed unnecessary JSON-encoding of parameter in Zulip 4.0 (feature level 64). 
   * @param params.leftSideUserlist Whether the users list on left sidebar in narrow windows.  This feature is not heavily used and is likely to be reworked. 
   * @param params.emojiset The user\&#39;s configured [emoji set](/help/emoji-and-emoticons#use-emoticons), used to display emoji to the user everything they appear in the UI.  * \&quot;google\&quot; - Google modern * \&quot;google-blob\&quot; - Google classic * \&quot;twitter\&quot; - Twitter * \&quot;text\&quot; - Plain text  **Changes**: Removed unnecessary JSON-encoding of parameter in Zulip 4.0 (feature level 64). 
   * @param params.demoteInactiveStreams Whether to [demote inactive streams](/help/manage-inactive-streams) in the left sidebar.  * 1 - Automatic * 2 - Always * 3 - Never 
   * @param params.timezone The user\&#39;s [configured timezone](/help/change-your-timezone).  Timezone values supported by the server are served at [/static/generated/timezones.json](/static/generated/timezones.json).  **Changes**: Removed unnecessary JSON-encoding of parameter in Zulip 4.0 (feature level 64). 
   */
  async updateDisplaySettings(params: IUpdateDisplaySettingsParams): Promise<JsonSuccessBase & object> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/settings/display`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPatch()
      // Set query parameters
      .withParams({ 
        'twenty_four_hour_time': params['twentyFourHourTime'],
        'dense_mode': params['denseMode'],
        'starred_message_counts': params['starredMessageCounts'],
        'fluid_layout_width': params['fluidLayoutWidth'],
        'high_contrast_mode': params['highContrastMode'],
        'color_scheme': params['colorScheme'],
        'translate_emoticons': params['translateEmoticons'],
        'default_language': params['defaultLanguage'],
        'default_view': params['defaultView'],
        'left_side_userlist': params['leftSideUserlist'],
        'emojiset': params['emojiset'],
        'demote_inactive_streams': params['demoteInactiveStreams'],
        'timezone': params['timezone'],
      })

      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Update notification settings
   * This endpoint is used to edit the user\&#39;s global notification settings. See [this endpoint](/api/update-subscription-settings) for per-stream notification settings.  &#x60;PATCH {{ api_url }}/v1/settings/notifications&#x60; 
   * @param params.enableStreamDesktopNotifications Enable visual desktop notifications for stream messages. 
   * @param params.enableStreamEmailNotifications Enable email notifications for stream messages. 
   * @param params.enableStreamPushNotifications Enable mobile notifications for stream messages. 
   * @param params.enableStreamAudibleNotifications Enable audible desktop notifications for stream messages. 
   * @param params.notificationSound Notification sound name.  **Changes**: Removed unnecessary JSON-encoding of parameter in Zulip 4.0 (feature level 63). 
   * @param params.enableDesktopNotifications Enable visual desktop notifications for private messages and @-mentions. 
   * @param params.enableSounds Enable audible desktop notifications for private messages and @-mentions. 
   * @param params.enableOfflineEmailNotifications Enable email notifications for private messages and @-mentions received when the user is offline. 
   * @param params.enableOfflinePushNotifications Enable mobile notification for private messages and @-mentions received when the user is offline. 
   * @param params.enableOnlinePushNotifications Enable mobile notification for private messages and @-mentions received when the user is online. 
   * @param params.enableDigestEmails Enable digest emails when the user is away. 
   * @param params.enableMarketingEmails Enable marketing emails. Has no function outside Zulip Cloud. 
   * @param params.enableLoginEmails Enable email notifications for new logins to account. 
   * @param params.messageContentInEmailNotifications Include the message\&#39;s content in email notifications for new messages. 
   * @param params.pmContentInDesktopNotifications Include content of private messages in desktop notifications. 
   * @param params.wildcardMentionsNotify Whether wildcard mentions (E.g. @**all**) should send notifications like a personal mention. 
   * @param params.desktopIconCountDisplay Unread count summary (appears in desktop sidebar and browser tab)  * 1 - All unreads * 2 - Private messages and mentions * 3 - None 
   * @param params.realmNameInNotifications Include organization name in subject of message notification emails. 
   * @param params.presenceEnabled Display the presence status to other users when online. 
   */
  async updateNotificationSettings(params: IUpdateNotificationSettingsParams): Promise<JsonSuccessBase & object> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/settings/notifications`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPatch()
      // Set query parameters
      .withParams({ 
        'enable_stream_desktop_notifications': params['enableStreamDesktopNotifications'],
        'enable_stream_email_notifications': params['enableStreamEmailNotifications'],
        'enable_stream_push_notifications': params['enableStreamPushNotifications'],
        'enable_stream_audible_notifications': params['enableStreamAudibleNotifications'],
        'notification_sound': params['notificationSound'],
        'enable_desktop_notifications': params['enableDesktopNotifications'],
        'enable_sounds': params['enableSounds'],
        'enable_offline_email_notifications': params['enableOfflineEmailNotifications'],
        'enable_offline_push_notifications': params['enableOfflinePushNotifications'],
        'enable_online_push_notifications': params['enableOnlinePushNotifications'],
        'enable_digest_emails': params['enableDigestEmails'],
        'enable_marketing_emails': params['enableMarketingEmails'],
        'enable_login_emails': params['enableLoginEmails'],
        'message_content_in_email_notifications': params['messageContentInEmailNotifications'],
        'pm_content_in_desktop_notifications': params['pmContentInDesktopNotifications'],
        'wildcard_mentions_notify': params['wildcardMentionsNotify'],
        'desktop_icon_count_display': params['desktopIconCountDisplay'],
        'realm_name_in_notifications': params['realmNameInNotifications'],
        'presence_enabled': params['presenceEnabled'],
      })

      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Update a user
   * Administrative endpoint to update the details of another user in the organization.  &#x60;PATCH {{ api_url }}/v1/users/{user_id}&#x60;  Supports everything an administrator can do to edit details of another user\&#39;s account, including editing full name, [role](/help/roles-and-permissions), and [custom profile fields](/help/add-custom-profile-fields). 
   * @param params.userId The target user\&#39;s ID. 
   * @param params.fullName The user\&#39;s full name. 
   * @param params.role New [role](/help/roles-and-permissions) for the user.  Roles are encoded as:  * Organization owner: 100 * Organization administrator: 200 * Organization moderator: 300 * Member: 400 * Guest: 600  Only organization owners can add or remove the owner role.  The owner role cannot be removed from the only organization owner.  **Changes**: New in Zulip 3.0 (feature level 8), replacing the previous pair of &#x60;is_admin&#x60; and &#x60;is_guest&#x60; boolean parameters. Organization moderator role added in Zulip 4.0 (feature level 60). 
   * @param params.profileData A dictionary containing the to be updated custom profile field data for the user. 
   */
  async updateUser(params: IUpdateUserParams): Promise<JsonSuccess> {
    // Verify required parameters are set
    this.ensureParamIsSet('updateUser', params, 'userId');

    // Create URL to call
    const url = `${this.basePath}/users/{user_id}`
      .replace(`{${'user_id'}}`, encodeURIComponent(`${params['userId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPatch()
      // Set query parameters
      .withParams({ 
        'full_name': params['fullName'],
        'role': params['role'],
        'profile_data': params['profileData'],
      })

      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Update a user group
   * Update the name or description of a [user group](/help/user-groups).  &#x60;PATCH {{ api_url }}/v1/user_groups/{user_group_id}&#x60; 
   * @param params.userGroupId The ID of the target user group. 
   * @param params.name The new name of the group. 
   * @param params.description The new description of the group. 
   */
  async updateUserGroup(params: IUpdateUserGroupParams): Promise<JsonSuccess> {
    // Verify required parameters are set
    this.ensureParamIsSet('updateUserGroup', params, 'userGroupId');
    this.ensureParamIsSet('updateUserGroup', params, 'name');
    this.ensureParamIsSet('updateUserGroup', params, 'description');

    // Create URL to call
    const url = `${this.basePath}/user_groups/{user_group_id}`
      .replace(`{${'user_group_id'}}`, encodeURIComponent(`${params['userGroupId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPatch()
      // Set query parameters
      .withParams({ 
        'name': params['name'],
        'description': params['description'],
      })

      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Update user group members
   * Update the members of a [user group](/help/user-groups).  &#x60;POST {{ api_url }}/v1/user_groups/{user_group_id}/members&#x60; 
   * @param params.userGroupId The ID of the target user group. 
   * @param params._delete The list of user ids to be removed from the user group. 
   * @param params.add The list of user ids to be added to the user group. 
   */
  async updateUserGroupMembers(params: IUpdateUserGroupMembersParams): Promise<JsonSuccess> {
    // Verify required parameters are set
    this.ensureParamIsSet('updateUserGroupMembers', params, 'userGroupId');

    // Create URL to call
    const url = `${this.basePath}/user_groups/{user_group_id}/members`
      .replace(`{${'user_group_id'}}`, encodeURIComponent(`${params['userGroupId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Set query parameters
      .withParams({ 
        'delete': params['_delete'],
        'add': params['add'],
      })

      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

}

