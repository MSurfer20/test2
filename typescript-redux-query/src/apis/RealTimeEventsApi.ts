// tslint:disable
/**
 * Zulip REST API
 * Powerful open source group chat 
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import { HttpMethods, QueryConfig, ResponseBody, ResponseText } from 'redux-query';
import * as runtime from '../runtime';
import {
    BadEventQueueIdError,
    BadEventQueueIdErrorFromJSON,
    BadEventQueueIdErrorToJSON,
    InvalidApiKeyError,
    InvalidApiKeyErrorFromJSON,
    InvalidApiKeyErrorToJSON,
    JsonSuccess,
    JsonSuccessFromJSON,
    JsonSuccessToJSON,
    JsonSuccessBase,
    JsonSuccessBaseFromJSON,
    JsonSuccessBaseToJSON,
    MissingArgumentError,
    MissingArgumentErrorFromJSON,
    MissingArgumentErrorToJSON,
    RateLimitedError,
    RateLimitedErrorFromJSON,
    RateLimitedErrorToJSON,
    RealmDeactivatedError,
    RealmDeactivatedErrorFromJSON,
    RealmDeactivatedErrorToJSON,
    UserDeactivatedError,
    UserDeactivatedErrorFromJSON,
    UserDeactivatedErrorToJSON,
    UserNotAuthorizedError,
    UserNotAuthorizedErrorFromJSON,
    UserNotAuthorizedErrorToJSON,
} from '../models';

export interface DeleteQueueRequest {
    queueId: string;
}

export interface GetEventsRequest {
    queueId: string;
    lastEventId?: number;
    dontBlock?: boolean;
}

export interface RealTimePostRequest {
    eventTypes?: Array<string>;
    narrow?: Array<Array<string>>;
    allPublicStreams?: boolean;
}

export interface RegisterQueueRequest {
    applyMarkdown?: boolean;
    clientGravatar?: boolean;
    slimPresence?: boolean;
    eventTypes?: Array<string>;
    allPublicStreams?: boolean;
    includeSubscribers?: boolean;
    clientCapabilities?: object;
    fetchEventTypes?: Array<string>;
    narrow?: Array<Array<string>>;
}


/**
 * Delete a previously registered queue.  `DELETE {{ api_url }}/v1/events` 
 * Delete an event queue
 */
function deleteQueueRaw<T>(requestParameters: DeleteQueueRequest, requestConfig: runtime.TypedQueryConfig<T, JsonSuccess> = {}): QueryConfig<T> {
    if (requestParameters.queueId === null || requestParameters.queueId === undefined) {
        throw new runtime.RequiredError('queueId','Required parameter requestParameters.queueId was null or undefined when calling deleteQueue.');
    }

    let queryParameters = null;

    queryParameters = {};


    if (requestParameters.queueId !== undefined) {
        queryParameters['queue_id'] = requestParameters.queueId;
    }

    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/events`,
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'DELETE',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(JsonSuccessFromJSON(body), text);
    }

    return config;
}

/**
* Delete a previously registered queue.  `DELETE {{ api_url }}/v1/events` 
* Delete an event queue
*/
export function deleteQueue<T>(requestParameters: DeleteQueueRequest, requestConfig?: runtime.TypedQueryConfig<T, JsonSuccess>): QueryConfig<T> {
    return deleteQueueRaw(requestParameters, requestConfig);
}

/**
 * `GET {{ api_url }}/v1/events`  This endpoint allows you to receive new events from [a registered event queue](/api/register-queue).  Long-lived clients should use the `event_queue_longpoll_timeout_seconds` property returned by `POST /register` as the client-side HTTP request timeout for calls to this endpoint.  It is guaranteed to be higher than heartbeat frequency and should be respected by clients to avoid breaking when heartbeat frequency increases. 
 * Get events from an event queue
 */
function getEventsRaw<T>(requestParameters: GetEventsRequest, requestConfig: runtime.TypedQueryConfig<T, JsonSuccessBase & object> = {}): QueryConfig<T> {
    if (requestParameters.queueId === null || requestParameters.queueId === undefined) {
        throw new runtime.RequiredError('queueId','Required parameter requestParameters.queueId was null or undefined when calling getEvents.');
    }

    let queryParameters = null;

    queryParameters = {};


    if (requestParameters.queueId !== undefined) {
        queryParameters['queue_id'] = requestParameters.queueId;
    }


    if (requestParameters.lastEventId !== undefined) {
        queryParameters['last_event_id'] = requestParameters.lastEventId;
    }


    if (requestParameters.dontBlock !== undefined) {
        queryParameters['dont_block'] = requestParameters.dontBlock;
    }

    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/events`,
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'GET',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(JsonSuccessBase &amp; objectFromJSON(body), text);
    }

    return config;
}

/**
* `GET {{ api_url }}/v1/events`  This endpoint allows you to receive new events from [a registered event queue](/api/register-queue).  Long-lived clients should use the `event_queue_longpoll_timeout_seconds` property returned by `POST /register` as the client-side HTTP request timeout for calls to this endpoint.  It is guaranteed to be higher than heartbeat frequency and should be respected by clients to avoid breaking when heartbeat frequency increases. 
* Get events from an event queue
*/
export function getEvents<T>(requestParameters: GetEventsRequest, requestConfig?: runtime.TypedQueryConfig<T, JsonSuccessBase & object>): QueryConfig<T> {
    return getEventsRaw(requestParameters, requestConfig);
}

/**
 * (Ignored) 
 */
function realTimePostRaw<T>(requestParameters: RealTimePostRequest, requestConfig: runtime.TypedQueryConfig<T, void> = {}): QueryConfig<T> {
    let queryParameters = null;

    queryParameters = {};


    if (requestParameters.eventTypes) {
        queryParameters['event_types'] = requestParameters.eventTypes;
    }


    if (requestParameters.narrow) {
        queryParameters['narrow'] = requestParameters.narrow;
    }


    if (requestParameters.allPublicStreams !== undefined) {
        queryParameters['all_public_streams'] = requestParameters.allPublicStreams;
    }

    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/real-time`,
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'POST',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
    }

    return config;
}

/**
* (Ignored) 
*/
export function realTimePost<T>(requestParameters: RealTimePostRequest, requestConfig?: runtime.TypedQueryConfig<T, void>): QueryConfig<T> {
    return realTimePostRaw(requestParameters, requestConfig);
}

/**
 * `POST {{ api_url }}/v1/register`  This powerful endpoint can be used to register a Zulip \"event queue\" (subscribed to certain types of \"events\", or updates to the messages and other Zulip data the current user has access to), as well as to fetch the current state of that data.  (`register` also powers the `call_on_each_event` Python API, and is intended primarily for complex applications for which the more convenient `call_on_each_event` API is insufficient).  This endpoint returns a `queue_id` and a `last_event_id`; these can be used in subsequent calls to the [\"events\" endpoint](/api/get-events) to request events from the Zulip server using long-polling.  The server will queue events for up to 10 minutes of inactivity. After 10 minutes, your event queue will be garbage-collected.  The server will send `heartbeat` events every minute, which makes it easy to implement a robust client that does not miss events unless the client loses network connectivity with the Zulip server for 10 minutes or longer.  Once the server garbage-collects your event queue, the server will [return an error](/api/get-events#bad_event_queue_id-errors) with a code of `BAD_EVENT_QUEUE_ID` if you try to fetch events from the event queue.  Your software will need to handle that error condition by re-initializing itself (e.g. this is what triggers your browser reloading the Zulip web app when your laptop comes back online after being offline for more than 10 minutes).  When prototyping with this API, we recommend first calling `register` with no `event_types` parameter to see all the available data from all supported event types.  Before using your client in production, you should set appropriate `event_types` and `fetch_event_types` filters so that your client only requests the data it needs.  A few minutes doing this often saves 90% of the total bandwidth and other resources consumed by a client using this API.  See the [events system developer documentation](https://zulip.readthedocs.io/en/latest/subsystems/events-system.html) if you need deeper details about how the Zulip event queue system works, avoids clients needing to worry about large classes of potentially messy races, etc. 
 * Register an event queue
 */
function registerQueueRaw<T>(requestParameters: RegisterQueueRequest, requestConfig: runtime.TypedQueryConfig<T, JsonSuccessBase & object> = {}): QueryConfig<T> {
    let queryParameters = null;

    queryParameters = {};


    if (requestParameters.applyMarkdown !== undefined) {
        queryParameters['apply_markdown'] = requestParameters.applyMarkdown;
    }


    if (requestParameters.clientGravatar !== undefined) {
        queryParameters['client_gravatar'] = requestParameters.clientGravatar;
    }


    if (requestParameters.slimPresence !== undefined) {
        queryParameters['slim_presence'] = requestParameters.slimPresence;
    }


    if (requestParameters.eventTypes) {
        queryParameters['event_types'] = requestParameters.eventTypes;
    }


    if (requestParameters.allPublicStreams !== undefined) {
        queryParameters['all_public_streams'] = requestParameters.allPublicStreams;
    }


    if (requestParameters.includeSubscribers !== undefined) {
        queryParameters['include_subscribers'] = requestParameters.includeSubscribers;
    }


    if (requestParameters.clientCapabilities !== undefined) {
        queryParameters['client_capabilities'] = requestParameters.clientCapabilities;
    }


    if (requestParameters.fetchEventTypes) {
        queryParameters['fetch_event_types'] = requestParameters.fetchEventTypes;
    }


    if (requestParameters.narrow) {
        queryParameters['narrow'] = requestParameters.narrow;
    }

    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/register`,
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'POST',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(JsonSuccessBase &amp; objectFromJSON(body), text);
    }

    return config;
}

/**
* `POST {{ api_url }}/v1/register`  This powerful endpoint can be used to register a Zulip \"event queue\" (subscribed to certain types of \"events\", or updates to the messages and other Zulip data the current user has access to), as well as to fetch the current state of that data.  (`register` also powers the `call_on_each_event` Python API, and is intended primarily for complex applications for which the more convenient `call_on_each_event` API is insufficient).  This endpoint returns a `queue_id` and a `last_event_id`; these can be used in subsequent calls to the [\"events\" endpoint](/api/get-events) to request events from the Zulip server using long-polling.  The server will queue events for up to 10 minutes of inactivity. After 10 minutes, your event queue will be garbage-collected.  The server will send `heartbeat` events every minute, which makes it easy to implement a robust client that does not miss events unless the client loses network connectivity with the Zulip server for 10 minutes or longer.  Once the server garbage-collects your event queue, the server will [return an error](/api/get-events#bad_event_queue_id-errors) with a code of `BAD_EVENT_QUEUE_ID` if you try to fetch events from the event queue.  Your software will need to handle that error condition by re-initializing itself (e.g. this is what triggers your browser reloading the Zulip web app when your laptop comes back online after being offline for more than 10 minutes).  When prototyping with this API, we recommend first calling `register` with no `event_types` parameter to see all the available data from all supported event types.  Before using your client in production, you should set appropriate `event_types` and `fetch_event_types` filters so that your client only requests the data it needs.  A few minutes doing this often saves 90% of the total bandwidth and other resources consumed by a client using this API.  See the [events system developer documentation](https://zulip.readthedocs.io/en/latest/subsystems/events-system.html) if you need deeper details about how the Zulip event queue system works, avoids clients needing to worry about large classes of potentially messy races, etc. 
* Register an event queue
*/
export function registerQueue<T>(requestParameters: RegisterQueueRequest, requestConfig?: runtime.TypedQueryConfig<T, JsonSuccessBase & object>): QueryConfig<T> {
    return registerQueueRaw(requestParameters, requestConfig);
}

/**
 * Common error to many endpoints 
 * Error handling
 */
function restErrorHandlingRaw<T>( requestConfig: runtime.TypedQueryConfig<T, void> = {}): QueryConfig<T> {
    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/rest-error-handling`,
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'POST',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
    }

    return config;
}

/**
* Common error to many endpoints 
* Error handling
*/
export function restErrorHandling<T>( requestConfig?: runtime.TypedQueryConfig<T, void>): QueryConfig<T> {
    return restErrorHandlingRaw( requestConfig);
}

