# Zulip REST API
#
# Powerful open source group chat 
#
# The version of the OpenAPI document: 1.0.0
# 
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title AddSubscriptionsResponse
#'
#' @description AddSubscriptionsResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field result  \link{AnyType} 
#'
#' @field msg  \link{AnyType} 
#'
#' @field subscribed  named list( \link{array[character]} ) [optional]
#'
#' @field already_subscribed  named list( \link{array[character]} ) [optional]
#'
#' @field unauthorized  list( character ) [optional]
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
AddSubscriptionsResponse <- R6::R6Class(
  'AddSubscriptionsResponse',
  public = list(
    `result` = NULL,
    `msg` = NULL,
    `subscribed` = NULL,
    `already_subscribed` = NULL,
    `unauthorized` = NULL,
    initialize = function(
        `result`, `msg`, `subscribed`=NULL, `already_subscribed`=NULL, `unauthorized`=NULL, ...
    ) {
      local.optional.var <- list(...)
      if (!missing(`result`)) {
        stopifnot(R6::is.R6(`result`))
        self$`result` <- `result`
      }
      if (!missing(`msg`)) {
        stopifnot(R6::is.R6(`msg`))
        self$`msg` <- `msg`
      }
      if (!is.null(`subscribed`)) {
        stopifnot(is.vector(`subscribed`), length(`subscribed`) != 0)
        sapply(`subscribed`, function(x) stopifnot(R6::is.R6(x)))
        self$`subscribed` <- `subscribed`
      }
      if (!is.null(`already_subscribed`)) {
        stopifnot(is.vector(`already_subscribed`), length(`already_subscribed`) != 0)
        sapply(`already_subscribed`, function(x) stopifnot(R6::is.R6(x)))
        self$`already_subscribed` <- `already_subscribed`
      }
      if (!is.null(`unauthorized`)) {
        stopifnot(is.vector(`unauthorized`), length(`unauthorized`) != 0)
        sapply(`unauthorized`, function(x) stopifnot(is.character(x)))
        self$`unauthorized` <- `unauthorized`
      }
    },
    toJSON = function() {
      AddSubscriptionsResponseObject <- list()
      if (!is.null(self$`result`)) {
        AddSubscriptionsResponseObject[['result']] <-
          self$`result`$toJSON()
      }
      if (!is.null(self$`msg`)) {
        AddSubscriptionsResponseObject[['msg']] <-
          self$`msg`$toJSON()
      }
      if (!is.null(self$`subscribed`)) {
        AddSubscriptionsResponseObject[['subscribed']] <-
          lapply(self$`subscribed`, function(x) x$toJSON())
      }
      if (!is.null(self$`already_subscribed`)) {
        AddSubscriptionsResponseObject[['already_subscribed']] <-
          lapply(self$`already_subscribed`, function(x) x$toJSON())
      }
      if (!is.null(self$`unauthorized`)) {
        AddSubscriptionsResponseObject[['unauthorized']] <-
          self$`unauthorized`
      }

      AddSubscriptionsResponseObject
    },
    fromJSON = function(AddSubscriptionsResponseJson) {
      AddSubscriptionsResponseObject <- jsonlite::fromJSON(AddSubscriptionsResponseJson)
      if (!is.null(AddSubscriptionsResponseObject$`result`)) {
        resultObject <- AnyType$new()
        resultObject$fromJSON(jsonlite::toJSON(AddSubscriptionsResponseObject$result, auto_unbox = TRUE, digits = NA))
        self$`result` <- resultObject
      }
      if (!is.null(AddSubscriptionsResponseObject$`msg`)) {
        msgObject <- AnyType$new()
        msgObject$fromJSON(jsonlite::toJSON(AddSubscriptionsResponseObject$msg, auto_unbox = TRUE, digits = NA))
        self$`msg` <- msgObject
      }
      if (!is.null(AddSubscriptionsResponseObject$`subscribed`)) {
        self$`subscribed` <- ApiClient$new()$deserializeObj(AddSubscriptionsResponseObject$`subscribed`, "map(array[character])", loadNamespace("openapi"))
      }
      if (!is.null(AddSubscriptionsResponseObject$`already_subscribed`)) {
        self$`already_subscribed` <- ApiClient$new()$deserializeObj(AddSubscriptionsResponseObject$`already_subscribed`, "map(array[character])", loadNamespace("openapi"))
      }
      if (!is.null(AddSubscriptionsResponseObject$`unauthorized`)) {
        self$`unauthorized` <- ApiClient$new()$deserializeObj(AddSubscriptionsResponseObject$`unauthorized`, "array[character]", loadNamespace("openapi"))
      }
      self
    },
    toJSONString = function() {
      jsoncontent <- c(
        if (!is.null(self$`result`)) {
        sprintf(
        '"result":
        %s
        ',
        jsonlite::toJSON(self$`result`$toJSON(), auto_unbox=TRUE, digits = NA)
        )},
        if (!is.null(self$`msg`)) {
        sprintf(
        '"msg":
        %s
        ',
        jsonlite::toJSON(self$`msg`$toJSON(), auto_unbox=TRUE, digits = NA)
        )},
        if (!is.null(self$`subscribed`)) {
        sprintf(
        '"subscribed":
        %s
',
        jsonlite::toJSON(lapply(self$`subscribed`, function(x){ x$toJSON() }), auto_unbox = TRUE, digits=NA)
        )},
        if (!is.null(self$`already_subscribed`)) {
        sprintf(
        '"already_subscribed":
        %s
',
        jsonlite::toJSON(lapply(self$`already_subscribed`, function(x){ x$toJSON() }), auto_unbox = TRUE, digits=NA)
        )},
        if (!is.null(self$`unauthorized`)) {
        sprintf(
        '"unauthorized":
           [%s]
        ',
        paste(unlist(lapply(self$`unauthorized`, function(x) paste0('"', x, '"'))), collapse=",")
        )}
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste('{', jsoncontent, '}', sep = "")
    },
    fromJSONString = function(AddSubscriptionsResponseJson) {
      AddSubscriptionsResponseObject <- jsonlite::fromJSON(AddSubscriptionsResponseJson)
      self$`result` <- AnyType$new()$fromJSON(jsonlite::toJSON(AddSubscriptionsResponseObject$result, auto_unbox = TRUE, digits = NA))
      self$`msg` <- AnyType$new()$fromJSON(jsonlite::toJSON(AddSubscriptionsResponseObject$msg, auto_unbox = TRUE, digits = NA))
      self$`subscribed` <- ApiClient$new()$deserializeObj(AddSubscriptionsResponseObject$`subscribed`, "map(array[character])", loadNamespace("openapi"))
      self$`already_subscribed` <- ApiClient$new()$deserializeObj(AddSubscriptionsResponseObject$`already_subscribed`, "map(array[character])", loadNamespace("openapi"))
      self$`unauthorized` <- ApiClient$new()$deserializeObj(AddSubscriptionsResponseObject$`unauthorized`, "array[character]", loadNamespace("openapi"))
      self
    }
  )
)
