# Automatically generated by openapi-generator (https://openapi-generator.tech)
# Please update as you see appropriate

context("Test ServerAndOrganizationsApi")

api.instance <- ServerAndOrganizationsApi$new()

test_that("AddCodePlayground", {
  # tests for AddCodePlayground
  # base path: https://example.zulipchat.com/api/v1
  # Add a code playground
  # Configure [code playgrounds](/help/code-blocks#code-playgrounds) for the organization.  &#x60;POST {{ api_url }}/v1/realm/playgrounds&#x60;  **Changes**: New in Zulip 4.0 (feature level 49). A parameter encoding bug was fixed in Zulip 4.0 (feature level 57). 
  # @param name character The user-visible display name of the playground which can be used to pick the target playground, especially when multiple playground options exist for that programming language. 
  # @param pygments.language character The name of the Pygments language lexer for that programming language. 
  # @param url.prefix character The url prefix for the playground. 
  # @return [JsonSuccessBase]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("AddLinkifier", {
  # tests for AddLinkifier
  # base path: https://example.zulipchat.com/api/v1
  # Add a linkifier
  # Configure [linkifiers](/help/add-a-custom-linkifier), regular expression patterns that are automatically linkified when they appear in messages and topics.  &#x60;POST {{ api_url }}/v1/realm/filters&#x60; 
  # @param pattern character The [Python regular expression](https://docs.python.org/3/howto/regex.html) that should trigger the linkifier. 
  # @param url.format.string character The URL used for the link. If you used named groups for the `pattern`, you can insert their content here with `%(name_of_the_capturing_group)s`. 
  # @return [JsonSuccessBase]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("CreateCustomProfileField", {
  # tests for CreateCustomProfileField
  # base path: https://example.zulipchat.com/api/v1
  # Create a custom profile field
  # [Create a custom profile field](/help/add-custom-profile-fields) in the user&#39;s organization.  &#x60;POST {{ api_url }}/v1/realm/profile_fields&#x60; 
  # @param field.type integer The field type can be any of the supported custom profile field types. See the [custom profile fields documentation](/help/add-custom-profile-fields) more details on what each type means.  * **1**: Short text * **2**: Long text * **3**: List of options * **4**: Date picker * **5**: Link * **6**: Person picker * **7**: External account 
  # @param name character The name of the custom profile field, which will appear both in user-facing settings UI for configuring custom profile fields and in UI displaying a user's profile.  (optional)
  # @param hint character The help text to be displayed for the custom profile field in user-facing settings UI for configuring custom profile fields.  (optional)
  # @param field.data object Field types 3 (List of options) and 7 (External account) support storing additional configuration for the field type in the `field_data` attribute.  For field type 3 (List of options), this attribute is a JSON dictionary defining the choices and the order they will be displayed in the dropdown UI for individual users to select an option.  The interface for field type 7 is not yet stabilized.  (optional)
  # @return [JsonSuccessBase]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("GetCustomEmoji", {
  # tests for GetCustomEmoji
  # base path: https://example.zulipchat.com/api/v1
  # Get all custom emoji
  # Get all the custom emoji in the user&#39;s organization.  &#x60;GET {{ api_url }}/v1/realm/emoji&#x60; 
  # @return [JsonSuccessBase]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("GetCustomProfileFields", {
  # tests for GetCustomProfileFields
  # base path: https://example.zulipchat.com/api/v1
  # Get all custom profile fields
  # Get all the [custom profile fields](/help/add-custom-profile-fields) configured for the user&#39;s organization.  &#x60;GET {{ api_url }}/v1/realm/profile_fields&#x60; 
  # @return [JsonSuccessBase]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("GetLinkifiers", {
  # tests for GetLinkifiers
  # base path: https://example.zulipchat.com/api/v1
  # Get linkifiers
  # List all of an organization&#39;s configured [linkifiers](/help/add-a-custom-linkifier), regular expression patterns that are automatically linkified when they appear in messages and topics.  &#x60;GET {{ api_url }}/v1/realm/linkifiers&#x60;  **Changes**: New in Zulip 4.0 (feature level 54). On older versions, a similar &#x60;GET /realm/filters&#x60; endpoint was available with each entry in a &#x60;[pattern, url_format, id]&#x60; tuple format. 
  # @return [JsonSuccessBase]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("GetServerSettings", {
  # tests for GetServerSettings
  # base path: https://example.zulipchat.com/api/v1
  # Get server settings
  # Fetch global settings for a Zulip server.  &#x60;GET {{ api_url }}/v1/server_settings&#x60;  **Note:** this endpoint does not require any authentication at all, and you can use it to check:  * If this is a Zulip server, and if so, what version of Zulip it&#39;s running. * What a Zulip client (e.g. a mobile app or [zulip-terminal](https://github.com/zulip/zulip-terminal/)) needs to know in order to display a login prompt for the server (e.g. what authentication methods are available). 
  # @return [JsonSuccessBase]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("RemoveCodePlayground", {
  # tests for RemoveCodePlayground
  # base path: https://example.zulipchat.com/api/v1
  # Remove a code playground
  # Remove a [code playground](/help/code-blocks#code-playgrounds) previously configured for an organization.  &#x60;DELETE {{ api_url }}/v1/realm/playgrounds/{playground_id}&#x60;  **Changes**: New in Zulip 4.0 (feature level 49). 
  # @param playground.id integer The ID of the playground that you want to remove. 
  # @return [JsonSuccess]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("RemoveLinkifier", {
  # tests for RemoveLinkifier
  # base path: https://example.zulipchat.com/api/v1
  # Remove a linkifier
  # Remove [linkifiers](/help/add-a-custom-linkifier), regular expression patterns that are automatically linkified when they appear in messages and topics.  &#x60;DELETE {{ api_url }}/v1/realm/filters/{filter_id}&#x60; 
  # @param filter.id integer The ID of the linkifier that you want to remove. 
  # @return [JsonSuccess]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("ReorderCustomProfileFields", {
  # tests for ReorderCustomProfileFields
  # base path: https://example.zulipchat.com/api/v1
  # Reorder custom profile fields
  # Reorder the custom profile fields in the user&#39;s organization.  &#x60;PATCH {{ api_url }}/v1/realm/profile_fields&#x60;  Custom profile fields are displayed in Zulip UI widgets in order; this endpoint allows administrative settings UI to change the field ordering.  This endpoint is used to implement the dragging feature described in the [custom profile fields documentation](/help/add-custom-profile-fields). 
  # @param order array[integer] A list of the IDs of all the custom profile fields defined in this organization, in the desired new order. 
  # @return [JsonSuccess]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("UpdateLinkifier", {
  # tests for UpdateLinkifier
  # base path: https://example.zulipchat.com/api/v1
  # Update a linkifier
  # Update a [linkifier](/help/add-a-custom-linkifier), regular expression patterns that are automatically linkified when they appear in messages and topics.  &#x60;PATCH {{ api_url }}/v1/realm/filters/{filter_id}&#x60;  **Changes**: New in Zulip 4.0 (feature level 57). 
  # @param filter.id integer The ID of the linkifier that you want to update. 
  # @param pattern character The [Python regular expression](https://docs.python.org/3/howto/regex.html) that should trigger the linkifier. 
  # @param url.format.string character The URL used for the link. If you used named groups for the `pattern`, you can insert their content here with `%(name_of_the_capturing_group)s`. 
  # @return [JsonSuccess]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("UploadCustomEmoji", {
  # tests for UploadCustomEmoji
  # base path: https://example.zulipchat.com/api/v1
  # Upload custom emoji
  # This endpoint is used to upload a custom emoji for use in the user&#39;s organization.  Access to this endpoint depends on the [organization&#39;s configuration](https://zulip.com/help/only-allow-admins-to-add-emoji).  &#x60;POST {{ api_url }}/v1/realm/emoji/{emoji_name}&#x60; 
  # @param emoji.name character The name that should be associated with the uploaded emoji image/gif. The emoji name can only contain letters, numbers, dashes, and spaces. Upper and lower case letters are treated the same, and underscores (_) are treated the same as spaces (consistent with how the Zulip UI handles emoji). 
  # @param filename data.frame  (optional)
  # @return [JsonSuccess]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

