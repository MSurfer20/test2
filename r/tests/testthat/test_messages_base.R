# Automatically generated by openapi-generator (https://openapi-generator.tech)
# Please update as you see appropriate

context("Test MessagesBase")

model.instance <- MessagesBase$new()

test_that("avatar_url", {
  # tests for the property `avatar_url` (character)
  # The URL of the user&#39;s avatar.  Can be null only if client_gravatar was passed, which means that the user has not uploaded an avatar in Zulip, and the client should compute the gravatar URL by hashing the user&#39;s email address itself for this user. 

  # uncomment below to test the property 
  #expect_equal(model.instance$`avatar_url`, "EXPECTED_RESULT")
})

test_that("client", {
  # tests for the property `client` (character)
  # A Zulip \&quot;client\&quot; string, describing what Zulip client sent the message. 

  # uncomment below to test the property 
  #expect_equal(model.instance$`client`, "EXPECTED_RESULT")
})

test_that("content", {
  # tests for the property `content` (character)
  # The content/body of the message. 

  # uncomment below to test the property 
  #expect_equal(model.instance$`content`, "EXPECTED_RESULT")
})

test_that("content_type", {
  # tests for the property `content_type` (character)
  # The HTTP &#x60;content_type&#x60; for the message content.  This will be &#x60;text/html&#x60; or &#x60;text/x-markdown&#x60;, depending on whether &#x60;apply_markdown&#x60; was set. 

  # uncomment below to test the property 
  #expect_equal(model.instance$`content_type`, "EXPECTED_RESULT")
})

test_that("display_recipient", {
  # tests for the property `display_recipient` (OneOfstringarray)
  # Data on the recipient of the message; either the name of a stream or a dictionary containing basic data on the users who received the message. 

  # uncomment below to test the property 
  #expect_equal(model.instance$`display_recipient`, "EXPECTED_RESULT")
})

test_that("id", {
  # tests for the property `id` (integer)
  # The unique message ID.  Messages should always be displayed sorted by ID. 

  # uncomment below to test the property 
  #expect_equal(model.instance$`id`, "EXPECTED_RESULT")
})

test_that("is_me_message", {
  # tests for the property `is_me_message` (character)
  # Whether the message is a [/me status message][status-messages]  [status-messages]: /help/format-your-message-using-markdown#status-messages 

  # uncomment below to test the property 
  #expect_equal(model.instance$`is_me_message`, "EXPECTED_RESULT")
})

test_that("reactions", {
  # tests for the property `reactions` (array[EmojiReaction])
  # Data on any reactions to the message. 

  # uncomment below to test the property 
  #expect_equal(model.instance$`reactions`, "EXPECTED_RESULT")
})

test_that("recipient_id", {
  # tests for the property `recipient_id` (integer)
  # A unique ID for the set of users receiving the message (either a stream or group of users).  Useful primarily for hashing. 

  # uncomment below to test the property 
  #expect_equal(model.instance$`recipient_id`, "EXPECTED_RESULT")
})

test_that("sender_email", {
  # tests for the property `sender_email` (character)
  # The Zulip display email address of the message&#39;s sender. 

  # uncomment below to test the property 
  #expect_equal(model.instance$`sender_email`, "EXPECTED_RESULT")
})

test_that("sender_full_name", {
  # tests for the property `sender_full_name` (character)
  # The full name of the message&#39;s sender. 

  # uncomment below to test the property 
  #expect_equal(model.instance$`sender_full_name`, "EXPECTED_RESULT")
})

test_that("sender_id", {
  # tests for the property `sender_id` (integer)
  # The user ID of the message&#39;s sender. 

  # uncomment below to test the property 
  #expect_equal(model.instance$`sender_id`, "EXPECTED_RESULT")
})

test_that("sender_realm_str", {
  # tests for the property `sender_realm_str` (character)
  # A string identifier for the realm the sender is in.  Unique only within the context of a given Zulip server.  E.g. on &#x60;example.zulip.com&#x60;, this will be &#x60;example&#x60;. 

  # uncomment below to test the property 
  #expect_equal(model.instance$`sender_realm_str`, "EXPECTED_RESULT")
})

test_that("stream_id", {
  # tests for the property `stream_id` (integer)
  # Only present for stream messages; the ID of the stream. 

  # uncomment below to test the property 
  #expect_equal(model.instance$`stream_id`, "EXPECTED_RESULT")
})

test_that("subject", {
  # tests for the property `subject` (character)
  # The &#x60;topic&#x60; of the message.  Currently always &#x60;\&quot;\&quot;&#x60; for private messages, though this could change if Zulip adds support for topics in private message conversations.  The field name is a legacy holdover from when topics were called \&quot;subjects\&quot; and will eventually change. 

  # uncomment below to test the property 
  #expect_equal(model.instance$`subject`, "EXPECTED_RESULT")
})

test_that("topic_links", {
  # tests for the property `topic_links` (array[MessagesBaseTopicLinks])
  # Data on any links to be included in the &#x60;topic&#x60; line (these are generated by [custom linkification filters](/help/add-a-custom-linkifier) that match content in the message&#39;s topic.)  **Changes**: This field contained a list of urls before   Zulip 4.0 (feature level 46).  New in Zulip 3.0 (feature level 1): Previously, this field was called &#x60;subject_links&#x60;; clients are recommended to rename &#x60;subject_links&#x60; to &#x60;topic_links&#x60; if present for compatibility with older Zulip servers. 

  # uncomment below to test the property 
  #expect_equal(model.instance$`topic_links`, "EXPECTED_RESULT")
})

test_that("submessages", {
  # tests for the property `submessages` (array[character])
  # Data used for certain experimental Zulip integrations. 

  # uncomment below to test the property 
  #expect_equal(model.instance$`submessages`, "EXPECTED_RESULT")
})

test_that("timestamp", {
  # tests for the property `timestamp` (integer)
  # The UNIX timestamp for when the message was sent, in UTC seconds. 

  # uncomment below to test the property 
  #expect_equal(model.instance$`timestamp`, "EXPECTED_RESULT")
})

test_that("type", {
  # tests for the property `type` (character)
  # The type of the message: &#x60;stream&#x60; or &#x60;private&#x60;. 

  # uncomment below to test the property 
  #expect_equal(model.instance$`type`, "EXPECTED_RESULT")
})

