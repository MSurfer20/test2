/**
* Zulip REST API
* Powerful open source group chat 
*
* The version of the OpenAPI document: 1.0.0
* 
*
* NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
* https://openapi-generator.tech
* Do not edit the class manually.
*/
package org.openapitools.database.models

import org.ktorm.dsl.*
import org.ktorm.schema.*
import org.ktorm.database.Database
import .*


/**
 * 
 * @param userId 
 * @param fullName 
 * @param apiKey 
 * @param defaultSendingStream 
 * @param defaultEventsRegisterStream 
 * @param defaultAllPublicStreams 
 * @param avatarUrl 
 * @param ownerId 
 * @param services 
 * @param email The email of the bot. 
 * @param botType An integer describing the type of bot: * `1` for a `Generic` bot. * `2` for an `Incoming webhook` bot. * `3` for an `Outgoing webhook` bot. * `4` for an `Embedded` bot. 
 * @param isActive A boolean describing whether the user account has been deactivated. 
 */
object Bots : BaseTable<Bot>("Bot") {
    val userId = blob("user_id") /* null */
    val fullName = blob("full_name") /* null */
    val apiKey = blob("api_key") /* null */
    val defaultSendingStream = blob("default_sending_stream") /* null */
    val defaultEventsRegisterStream = blob("default_events_register_stream") /* null */
    val defaultAllPublicStreams = blob("default_all_public_streams") /* null */
    val avatarUrl = blob("avatar_url") /* null */
    val ownerId = blob("owner_id") /* null */
    val services = blob("services") /* null */
    val email = text("email") /* null */ /* The email of the bot.  */
    val botType = int("bot_type") /* null */ /* An integer describing the type of bot: * `1` for a `Generic` bot. * `2` for an `Incoming webhook` bot. * `3` for an `Outgoing webhook` bot. * `4` for an `Embedded` bot.  */
    val isActive = boolean("is_active") /* null */ /* A boolean describing whether the user account has been deactivated.  */

    /**
     * Create an entity of type Bot from the model
     */
    override fun doCreateEntity(row: QueryRowSet, withReferences: Boolean) = Bot(
        userId = row[userId]  /* kotlin.Any? */,
        fullName = row[fullName]  /* kotlin.Any? */,
        apiKey = row[apiKey]  /* kotlin.Any? */,
        defaultSendingStream = row[defaultSendingStream]  /* kotlin.Any? */,
        defaultEventsRegisterStream = row[defaultEventsRegisterStream]  /* kotlin.Any? */,
        defaultAllPublicStreams = row[defaultAllPublicStreams]  /* kotlin.Any? */,
        avatarUrl = row[avatarUrl]  /* kotlin.Any? */,
        ownerId = row[ownerId]  /* kotlin.Any? */,
        services = row[services]  /* kotlin.Any? */,
        email = row[email]  /* kotlin.String? */ /* The email of the bot.  */,
        botType = row[botType]  /* kotlin.Int? */ /* An integer describing the type of bot: * `1` for a `Generic` bot. * `2` for an `Incoming webhook` bot. * `3` for an `Outgoing webhook` bot. * `4` for an `Embedded` bot.  */,
        isActive = row[isActive]  /* kotlin.Boolean? */ /* A boolean describing whether the user account has been deactivated.  */
    )

    /**
    * Assign all the columns from the entity of type Bot to the DML expression.
    *
    * Usage:
    *
    * ```kotlin
    * let entity = Bot()
    * database.update(Bots, {
    *     assignFrom(entity)
    * })
    * ```
    * @return the builder with the columns for the update or insert.
    */
    fun AssignmentsBuilder.assignFrom(entity: Bot) {
        this.apply {
            set(Bots.userId, entity.userId)
            set(Bots.fullName, entity.fullName)
            set(Bots.apiKey, entity.apiKey)
            set(Bots.defaultSendingStream, entity.defaultSendingStream)
            set(Bots.defaultEventsRegisterStream, entity.defaultEventsRegisterStream)
            set(Bots.defaultAllPublicStreams, entity.defaultAllPublicStreams)
            set(Bots.avatarUrl, entity.avatarUrl)
            set(Bots.ownerId, entity.ownerId)
            set(Bots.services, entity.services)
            set(Bots.email, entity.email)
            set(Bots.botType, entity.botType)
            set(Bots.isActive, entity.isActive)
        }
    }

}


