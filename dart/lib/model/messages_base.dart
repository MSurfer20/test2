//
// AUTO-GENERATED FILE, DO NOT MODIFY!
//
// @dart=2.0

// ignore_for_file: unused_element, unused_import
// ignore_for_file: always_put_required_named_parameters_first
// ignore_for_file: lines_longer_than_80_chars

part of openapi.api;

class MessagesBase {
  /// Returns a new [MessagesBase] instance.
  MessagesBase({
    this.avatarUrl,
    this.client,
    this.content,
    this.contentType,
    this.displayRecipient,
    this.id,
    this.isMeMessage,
    this.reactions = const [],
    this.recipientId,
    this.senderEmail,
    this.senderFullName,
    this.senderId,
    this.senderRealmStr,
    this.streamId,
    this.subject,
    this.topicLinks = const [],
    this.submessages = const [],
    this.timestamp,
    this.type,
  });

  /// The URL of the user's avatar.  Can be null only if client_gravatar was passed, which means that the user has not uploaded an avatar in Zulip, and the client should compute the gravatar URL by hashing the user's email address itself for this user. 
  String avatarUrl;

  /// A Zulip \"client\" string, describing what Zulip client sent the message. 
  String client;

  /// The content/body of the message. 
  String content;

  /// The HTTP `content_type` for the message content.  This will be `text/html` or `text/x-markdown`, depending on whether `apply_markdown` was set. 
  String contentType;

  /// Data on the recipient of the message; either the name of a stream or a dictionary containing basic data on the users who received the message. 
  OneOfstringarray displayRecipient;

  /// The unique message ID.  Messages should always be displayed sorted by ID. 
  int id;

  /// Whether the message is a [/me status message][status-messages]  [status-messages]: /help/format-your-message-using-markdown#status-messages 
  bool isMeMessage;

  /// Data on any reactions to the message. 
  List<EmojiReaction> reactions;

  /// A unique ID for the set of users receiving the message (either a stream or group of users).  Useful primarily for hashing. 
  int recipientId;

  /// The Zulip display email address of the message's sender. 
  String senderEmail;

  /// The full name of the message's sender. 
  String senderFullName;

  /// The user ID of the message's sender. 
  int senderId;

  /// A string identifier for the realm the sender is in.  Unique only within the context of a given Zulip server.  E.g. on `example.zulip.com`, this will be `example`. 
  String senderRealmStr;

  /// Only present for stream messages; the ID of the stream. 
  int streamId;

  /// The `topic` of the message.  Currently always `\"\"` for private messages, though this could change if Zulip adds support for topics in private message conversations.  The field name is a legacy holdover from when topics were called \"subjects\" and will eventually change. 
  String subject;

  /// Data on any links to be included in the `topic` line (these are generated by [custom linkification filters](/help/add-a-custom-linkifier) that match content in the message's topic.)  **Changes**: This field contained a list of urls before   Zulip 4.0 (feature level 46).  New in Zulip 3.0 (feature level 1): Previously, this field was called `subject_links`; clients are recommended to rename `subject_links` to `topic_links` if present for compatibility with older Zulip servers. 
  List<MessagesBaseTopicLinks> topicLinks;

  /// Data used for certain experimental Zulip integrations. 
  List<String> submessages;

  /// The UNIX timestamp for when the message was sent, in UTC seconds. 
  int timestamp;

  /// The type of the message: `stream` or `private`. 
  String type;

  @override
  bool operator ==(Object other) => identical(this, other) || other is MessagesBase &&
     other.avatarUrl == avatarUrl &&
     other.client == client &&
     other.content == content &&
     other.contentType == contentType &&
     other.displayRecipient == displayRecipient &&
     other.id == id &&
     other.isMeMessage == isMeMessage &&
     other.reactions == reactions &&
     other.recipientId == recipientId &&
     other.senderEmail == senderEmail &&
     other.senderFullName == senderFullName &&
     other.senderId == senderId &&
     other.senderRealmStr == senderRealmStr &&
     other.streamId == streamId &&
     other.subject == subject &&
     other.topicLinks == topicLinks &&
     other.submessages == submessages &&
     other.timestamp == timestamp &&
     other.type == type;

  @override
  int get hashCode =>
    (avatarUrl == null ? 0 : avatarUrl.hashCode) +
    (client == null ? 0 : client.hashCode) +
    (content == null ? 0 : content.hashCode) +
    (contentType == null ? 0 : contentType.hashCode) +
    (displayRecipient == null ? 0 : displayRecipient.hashCode) +
    (id == null ? 0 : id.hashCode) +
    (isMeMessage == null ? 0 : isMeMessage.hashCode) +
    (reactions == null ? 0 : reactions.hashCode) +
    (recipientId == null ? 0 : recipientId.hashCode) +
    (senderEmail == null ? 0 : senderEmail.hashCode) +
    (senderFullName == null ? 0 : senderFullName.hashCode) +
    (senderId == null ? 0 : senderId.hashCode) +
    (senderRealmStr == null ? 0 : senderRealmStr.hashCode) +
    (streamId == null ? 0 : streamId.hashCode) +
    (subject == null ? 0 : subject.hashCode) +
    (topicLinks == null ? 0 : topicLinks.hashCode) +
    (submessages == null ? 0 : submessages.hashCode) +
    (timestamp == null ? 0 : timestamp.hashCode) +
    (type == null ? 0 : type.hashCode);

  @override
  String toString() => 'MessagesBase[avatarUrl=$avatarUrl, client=$client, content=$content, contentType=$contentType, displayRecipient=$displayRecipient, id=$id, isMeMessage=$isMeMessage, reactions=$reactions, recipientId=$recipientId, senderEmail=$senderEmail, senderFullName=$senderFullName, senderId=$senderId, senderRealmStr=$senderRealmStr, streamId=$streamId, subject=$subject, topicLinks=$topicLinks, submessages=$submessages, timestamp=$timestamp, type=$type]';

  Map<String, dynamic> toJson() {
    final json = <String, dynamic>{};
    if (avatarUrl != null) {
      json[r'avatar_url'] = avatarUrl;
    }
    if (client != null) {
      json[r'client'] = client;
    }
    if (content != null) {
      json[r'content'] = content;
    }
    if (contentType != null) {
      json[r'content_type'] = contentType;
    }
    if (displayRecipient != null) {
      json[r'display_recipient'] = displayRecipient;
    }
    if (id != null) {
      json[r'id'] = id;
    }
    if (isMeMessage != null) {
      json[r'is_me_message'] = isMeMessage;
    }
    if (reactions != null) {
      json[r'reactions'] = reactions;
    }
    if (recipientId != null) {
      json[r'recipient_id'] = recipientId;
    }
    if (senderEmail != null) {
      json[r'sender_email'] = senderEmail;
    }
    if (senderFullName != null) {
      json[r'sender_full_name'] = senderFullName;
    }
    if (senderId != null) {
      json[r'sender_id'] = senderId;
    }
    if (senderRealmStr != null) {
      json[r'sender_realm_str'] = senderRealmStr;
    }
    if (streamId != null) {
      json[r'stream_id'] = streamId;
    }
    if (subject != null) {
      json[r'subject'] = subject;
    }
    if (topicLinks != null) {
      json[r'topic_links'] = topicLinks;
    }
    if (submessages != null) {
      json[r'submessages'] = submessages;
    }
    if (timestamp != null) {
      json[r'timestamp'] = timestamp;
    }
    if (type != null) {
      json[r'type'] = type;
    }
    return json;
  }

  /// Returns a new [MessagesBase] instance and imports its values from
  /// [json] if it's non-null, null if [json] is null.
  static MessagesBase fromJson(Map<String, dynamic> json) => json == null
    ? null
    : MessagesBase(
        avatarUrl: json[r'avatar_url'],
        client: json[r'client'],
        content: json[r'content'],
        contentType: json[r'content_type'],
        displayRecipient: OneOfstringarray.fromJson(json[r'display_recipient']),
        id: json[r'id'],
        isMeMessage: json[r'is_me_message'],
        reactions: EmojiReaction.listFromJson(json[r'reactions']),
        recipientId: json[r'recipient_id'],
        senderEmail: json[r'sender_email'],
        senderFullName: json[r'sender_full_name'],
        senderId: json[r'sender_id'],
        senderRealmStr: json[r'sender_realm_str'],
        streamId: json[r'stream_id'],
        subject: json[r'subject'],
        topicLinks: MessagesBaseTopicLinks.listFromJson(json[r'topic_links']),
        submessages: json[r'submessages'] == null
          ? null
          : (json[r'submessages'] as List).cast<String>(),
        timestamp: json[r'timestamp'],
        type: json[r'type'],
    );

  static List<MessagesBase> listFromJson(List<dynamic> json, {bool emptyIsNull, bool growable,}) =>
    json == null || json.isEmpty
      ? true == emptyIsNull ? null : <MessagesBase>[]
      : json.map((dynamic value) => MessagesBase.fromJson(value)).toList(growable: true == growable);

  static Map<String, MessagesBase> mapFromJson(Map<String, dynamic> json) {
    final map = <String, MessagesBase>{};
    if (json?.isNotEmpty == true) {
      json.forEach((key, value) => map[key] = MessagesBase.fromJson(value));
    }
    return map;
  }

  // maps a json object with a list of MessagesBase-objects as value to a dart map
  static Map<String, List<MessagesBase>> mapListFromJson(Map<String, dynamic> json, {bool emptyIsNull, bool growable,}) {
    final map = <String, List<MessagesBase>>{};
    if (json?.isNotEmpty == true) {
      json.forEach((key, value) {
        map[key] = MessagesBase.listFromJson(value, emptyIsNull: emptyIsNull, growable: growable,);
      });
    }
    return map;
  }
}

